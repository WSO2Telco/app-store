import { FrameManagerDirective } from './frame-manager.directive';

describe('FrameManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new FrameManagerDirective();
    expect(directive).toBeTruthy();
  });
});
